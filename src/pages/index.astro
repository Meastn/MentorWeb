---
/* REF: MG-2026-ASTRO-INDEX-REDIRECT-03 */
---
<!DOCTYPE html>
<html lang="tr">
  <head>
    <meta charset="utf-8" />
    <title>Mentor Group Advisory</title>
    <!-- Server/Static Redirect Fallback (defaults to Turkish for SEO crawling) -->
    <meta http-equiv="refresh" content="0;url=/tr/" />
    <link rel="canonical" href="https://www.mentorgroup.com.tr/tr/" />
    <meta name="robots" content="noindex, follow" />
    
    <script is:inline>
      (async function() {
        try {
          // 1. Prioritize fast GeoIP check so VPN testing works perfectly
          const controller = new AbortController();
          const timeoutId = setTimeout(() => controller.abort(), 800); // 800ms strict timeout for high-speed routing
          
          try {
            const res = await fetch('https://ipapi.co/country/', { signal: controller.signal });
            const country = (await res.text()).trim().toUpperCase();
            clearTimeout(timeoutId);
            
            if (country === 'TR') {
              window.location.replace('/tr/');
            } else {
              window.location.replace('/en/');
            }
          } catch (apiErr) {
            // 2. Fallback to Browser Preference if API fails or times out
            const userLang = (navigator.language || navigator.userLanguage || '').toLowerCase();
            if (userLang.startsWith('tr')) {
              window.location.replace('/tr/');
            } else {
              window.location.replace('/en/');
            }
          }
        } catch (e) {
          // Absolute fallback
          window.location.replace('/tr/');
        }
      })();
    </script>
  </head>
  <body style="background-color: #FAF7F2; display: none;">
    <!-- Keeps the screen perfectly clean and invisible during the millisecond redirection phase -->
  </body>
</html>
